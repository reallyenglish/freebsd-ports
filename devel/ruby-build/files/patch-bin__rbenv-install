--- bin/rbenv-install.orig	2015-08-03 12:55:42 UTC
+++ bin/rbenv-install
@@ -42,6 +42,10 @@ if [ "$1" = "--complete" ]; then
   exec ruby-build --definitions
 fi
 
+if [ `uname -s` = "FreeBSD" ]; then
+  export MAKE="gmake"
+fi
+
 # Load shared library functions
 eval "$(ruby-build --lib)"
 
