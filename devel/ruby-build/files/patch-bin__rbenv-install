--- bin/rbenv-install.orig	2016-01-11 16:07:12 UTC
+++ bin/rbenv-install
@@ -48,6 +48,10 @@ if [ "$1" = "--complete" ]; then
   exec ruby-build --definitions
 fi
 
+if [ `uname -s` = "FreeBSD" ]; then
+  export MAKE="gmake"
+fi
+
 # Load shared library functions
 eval "$(ruby-build --lib)"
 
