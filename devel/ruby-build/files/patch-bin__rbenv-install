--- bin/rbenv-install.orig	2017-05-23 14:39:10 UTC
+++ bin/rbenv-install
@@ -47,6 +47,10 @@ if [ "$1" = "--complete" ]; then
   exec ruby-build --definitions
 fi
 
+if [ `uname -s` = "FreeBSD" ]; then
+  export MAKE="gmake"
+fi
+
 # Load shared library functions
 eval "$(ruby-build --lib)"
 
